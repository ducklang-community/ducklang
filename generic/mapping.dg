

resultFromEach with iterator, mapping, length:
	for each x in iterator, result having size length
		collect mapping with x


resultFromEach with numbers, mapping:
    iterator = getIterator with numbers
    length = getLength with numbers
	result resultFromEach with iterator, mapping, length

